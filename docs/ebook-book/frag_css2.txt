
/* 여덟 단계 한 바퀴 */
.cyc-fig{margin:24px 0;padding:0}
.cyc-fig svg{width:100%;height:auto;display:block;border:1px solid var(--line);
  border-radius:18px;background:var(--bg-2);padding:8px 0}
.cyc-fig figcaption{font-size:13.5px;color:var(--g600);margin-top:9px;font-weight:600;line-height:1.6}
.cyc-fig figcaption b{color:var(--g800)}
.cyc-box{fill:var(--bg);stroke:var(--line);stroke-width:1.6}
.cyc-hi{fill:var(--blue-fill);stroke:var(--blue);stroke-width:2}
.cyc-n{fill:var(--blue);font-size:22px;font-weight:800}
.cyc-nh{fill:var(--blue-ink)}
.cyc-t{fill:var(--g900);font-size:21px;font-weight:800;letter-spacing:-.03em}
.cyc-th{fill:var(--blue-ink)}
.cyc-w{fill:var(--g700);font-size:15px;font-weight:500}
.cyc-wh{fill:var(--g800)}
.cyc-s{fill:var(--blue);font-size:13.5px;font-weight:700}
.cyc-sh{fill:var(--blue-ink)}
.cyc-ln{stroke:var(--line-2);stroke-width:2.6;fill:none}
.cyc-back{stroke:var(--green);stroke-width:2.8;fill:none;stroke-dasharray:9 6}
.cyc-head{fill:var(--line-2)} .cyc-head2{fill:var(--green)}
.cyc-lb{fill:var(--green);font-size:14.5px;font-weight:700}
.cyc-cap{fill:var(--g700);font-size:16px;font-weight:700}
.cyc-cap2{fill:var(--g600);font-size:14px;font-weight:500}

/* 화면 사진에 번호 찍기 */
.pw{margin:20px 0 4px;padding:0}
.pw-i{position:relative;display:block;line-height:0}
.pw-i img{width:100%;height:auto;display:block;border:1px solid var(--line);
  border-radius:14px;background:#fff}
.pin{
  position:absolute;transform:translate(-50%,-50%);
  min-width:27px;height:27px;padding:0 6px;border-radius:999px;
  background:var(--blue);color:#fff;font-size:14px;font-weight:800;line-height:27px;
  text-align:center;letter-spacing:-.02em;
  box-shadow:0 0 0 3px #fff, 0 2px 7px rgba(0,0,0,.34);
  font-variant-numeric:tabular-nums;
}
.pw figcaption{font-size:13.5px;color:var(--g600);margin-top:10px;font-weight:600;line-height:1.6}
.pw figcaption b{color:var(--g800)}
.pw + .ms-do{margin-top:14px}
.pw + .ms-do ol{padding-left:22px}
.pw + .ms-do li{margin:9px 0}

@media print{
  .cyc-fig,.pw,.pw + .ms-do{break-inside:avoid}
  .cyc-fig svg{background:#fff}
  .pin{-webkit-print-color-adjust:exact;print-color-adjust:exact;
    background:#3182F6 !important;color:#fff !important;box-shadow:0 0 0 2px #fff}
}

/* 두 사람의 지도 */
.mp-fig svg{padding:10px 0}
.mp-hub{stroke-width:2}
.mp-hub.mp-a{fill:#101B29;stroke:#101B29}
.mp-hub.mp-b{fill:var(--blue);stroke:var(--blue)}
.mp-hi{fill:#8FA9C4;font-size:14px;font-weight:700;letter-spacing:.04em}
.mp-hn{fill:#fff;font-size:25px;font-weight:800;letter-spacing:-.035em}
.mp-hs{fill:#B9CBDD;font-size:14.5px;font-weight:600}
.mp-ln{stroke:var(--line-2);stroke-width:2.2;fill:none}
.mp-box{fill:var(--bg);stroke:var(--line);stroke-width:1.6}
.mp-t{fill:var(--g900);font-size:20px;font-weight:800;letter-spacing:-.032em}
.mp-s{font-size:14.5px;font-weight:600}
.mp-a-s{fill:var(--g600)} .mp-b-s{fill:var(--blue)}
.mp-x1{stroke:var(--g700);stroke-width:2.4}
.mp-x2{stroke:var(--blue);stroke-width:2.4;stroke-dasharray:9 6}
.mp-hd1{fill:var(--g700)} .mp-hd2{fill:var(--blue)}
.mp-xl{font-size:15px;font-weight:700}
.mp-xl1{fill:var(--g700)} .mp-xl2{fill:var(--blue-ink)}
.mp-note{fill:var(--g500);font-size:14px;font-weight:600}

/* 값 지도 · 단계판 */
.op-w{fill:var(--g700);font-size:15.5px;font-weight:600}
.st-d{font-size:11.5px;font-weight:700;opacity:.8;letter-spacing:.03em}
.st-lb{fill:var(--g600);font-size:14.5px;font-weight:600}
