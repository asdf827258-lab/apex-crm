/api/generate      /.netlify/functions/generate      200
/api/gemini        /.netlify/functions/gemini        200
/api/toss-confirm  /.netlify/functions/toss-confirm  200
/api/toss-billing  /.netlify/functions/toss-billing  200
/api/market        /.netlify/functions/market        200
/api/market-daily  /.netlify/functions/market-daily  200
# invest-monthly · invest-daily · market-daily · ai-daily 는 여기에 두지 않는다.
# netlify.toml 에 schedule 이 걸린 함수는 Netlify 가 HTTP 호출을 막는다(403).
# 주소를 열어 두면 눌렀을 때 403 만 나와서, 고장 난 것처럼 보인다.
# 예약은 정상으로 돈다 — 손으로 돌려 봐야 하면 Netlify 대시보드의
# Functions → 해당 함수 → Run 을 쓴다.
# 보장분석 랜딩은 여기서 서비스하지 않는다 — Netlify 사이트를 따로 두었다.
# (Base directory = 보장분석). 광고 도착지는 그쪽 주소를 쓴다.
# 이 사이트로 들어온 옛 주소는 랜딩으로 보낸다 — 배포 후 아래 [ ] 를 채운다.
# /보장분석  https://[랜딩주소]/  301!
# /bojang    https://[랜딩주소]/  301!

/trading           /trading/index.html               200
/trading/bot       /trading/bot.html                 200
/trading/todo      /trading/todo.html                200
/할일              /trading/todo.html                200

/*    /index.html   200
