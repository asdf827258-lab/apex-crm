/* ══ 야간 자동 근무 — <b>예약으로 부르는 자리</b> ═══════════════════
 *
 * 일은 ai-daily.js 가 합니다. 여기는 <b>예약만</b> 받는 껍데기입니다.
 *
 * ★ <b>왜 나눴나.</b> Netlify 는 예약(schedule)으로 등록한 함수를 HTTP 로
 *   못 부르게 막습니다 — 403 을 돌려줍니다. ai-daily 에 예약이 걸려 있어서,
 *   앱의 「서버 야간 근무가 도는지 지금 눌러 보기」(nwRun)가 <b>줄곧 403</b>
 *   을 받고 있었습니다. 화면에는 「HTTP 403」 이라고만 떠서 왜인지 알 수가
 *   없었습니다. 예약은 여기로 옮기고 ai-daily 는 앱이 부를 수 있게 둡니다.
 *
 * ★ <b>일을 여기 또 적지 않습니다</b> (5번). 두 벌이 되면 한쪽만 고쳐집니다.
 *   check-push 가 「앱이 부르는 함수에 예약이 걸려 있지 않은지」 를 지킵니다.
 */
exports.handler = require('./ai-daily.js').handler;
