# APEX DB CRM 서버형 설치 안내

이 버전은 각 직원이 이메일과 비밀번호로 로그인하고, 담당자별 데이터가 Supabase 서버에 저장되는 버전입니다.

## 권한 구조

- 관리자
  - 모든 DB 조회
  - DB 등록·수정·삭제
  - 담당자 배정
  - 전체 통계 조회
- 담당자
  - 자신에게 배정된 DB만 조회
  - TA 결과와 부재 기록 입력
  - 본인 이름과 작성시간 자동 기록
  - 단톡 보고·녹음 전달 상태 관리

## 1. Supabase 프로젝트 만들기

1. Supabase에서 새 프로젝트를 만듭니다.
2. SQL Editor를 엽니다.
3. `supabase_schema.sql` 내용을 전부 붙여넣고 실행합니다.
4. Authentication > Providers에서 Email 로그인을 활성화합니다.
5. 초기 테스트 때 이메일 인증이 불편하면 Confirm email을 잠시 해제할 수 있습니다.

## 2. 최초 관리자 만들기

1. `index.html`을 열고 직원 등록 화면에서 본부장 계정을 먼저 등록합니다.
2. Supabase SQL Editor에서 아래 문장을 실제 이메일로 바꿔 실행합니다.

```sql
update public.profiles p
set role = 'admin'
from auth.users u
where p.id = u.id
  and u.email = '관리자이메일@example.com';
```

3. 로그아웃 후 다시 로그인하면 관리자 화면으로 변경됩니다.

## 3. 웹파일 연결

1. Supabase Dashboard > Project Settings > API에서 다음 두 값을 확인합니다.
   - Project URL
   - anon 또는 publishable key
2. `config.js`를 메모장으로 열어 두 값을 입력합니다.
3. `index.html`, `config.js`를 함께 배포합니다.
4. 절대로 service_role key를 입력하지 마세요.

`config.js`를 수정하지 않은 경우 각 브라우저에서 최초 연결 화면에 값을 입력하는 방식도 사용할 수 있습니다.

## 4. 배포 권장 방법

`index.html` 하나를 아래 서비스 중 하나에 업로드하면 직원들이 같은 주소로 접속할 수 있습니다.

- Netlify Drop
- Cloudflare Pages
- GitHub Pages
- 사내 웹서버

배포 후 Supabase Authentication URL Configuration의 Site URL에 실제 CRM 주소를 입력하세요.

## 5. 직원 사용 순서

1. 직원 등록에서 이름·이메일·비밀번호 입력
2. 관리자 로그인
3. DB 등록 후 담당자 배정
4. 직원이 본인 계정으로 로그인
5. 자신에게 배정된 DB에서 TA 결과 입력
6. 기록에는 로그인한 직원 이름과 시간이 자동 저장

## 보안

DB와 통화기록은 Row Level Security 정책으로 보호됩니다. 담당자는 자신에게 배정된 DB만 조회할 수 있고, 관리자는 전체 데이터를 조회합니다.
