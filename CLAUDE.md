# APEX YUN PRO — 고치기 전에 읽는 것

설계사 한 사람이 고객 앞에서 여는 화면입니다. **화면에 찍힌 숫자가 틀리면
그 자리에서 계약이 깨집니다.** 여기 적힌 것은 취향이 아니라, 실제로 그렇게
깨져 본 자리들입니다.

---

## 0. 손대기 전 · 손댄 뒤

```bash
export NODE_PATH=/opt/node22/lib/node_modules PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers

node scripts/check-twins.js      # 몇 초 — 같은 것이 두 곳에 있나
node scripts/check-html.js app/index.html app/finance.html   # 문법·함수 이름 중복
node scripts/check-sane.js       # 화면에 말이 안 되는 숫자가 찍혔나
node scripts/smoke.js            # 63개 화면이 실제로 열리나

node scripts/lane.js             # 밀기 전 — 다른 세션과 부딪히나 (11번)
```

앞의 둘은 브라우저를 안 띄워 **몇 초**면 끝납니다. 고치는 중에 계속 돌리십시오.

CI 전체는 **5~7분** 걸립니다 — 점검 133가지를 여섯 갈래로 나눠 동시에 돕니다.
문법이 깨졌으면 **30초** 만에 빨간불이 뜹니다(빠른 갈래가 먼저 돕니다).

```bash
node scripts/run-checks.js --group fast     # 브라우저 없는 것만 — 몇 초
node scripts/run-checks.js --shard 3/6      # CI 의 3번 갈래만 그대로
node scripts/run-checks.js --only nav,공지   # 이름에 그 글자가 든 것만
```

---

## 1. 지어내지 않는다 — 제일 중요한 규칙

이 앱은 고객에게 **돈** 이야기를 합니다. 모르는 값을 그럴듯하게 채우면
고객이 그것을 사실로 믿습니다.

- **제안서에 없는 값을 만들어 쓰지 않는다.** 5년납 표만 있으면 7년납 표는
  만들지 않는다 — 사업비·해지공제가 달라 곡선 자체가 다른 표다.
- **「모름(null)」과 「0」을 구분한다.** 못 읽은 것을 0으로 적으면
  「보장이 없다」는 뜻이 되어 버린다.
- **값이 없으면 화면을 세우지 않는다.** 「아무 숫자도 만들지 않습니다」라고
  적고, 무엇을 채워야 하는지 말한다.
- **평균으로 채운 값은 평균이라고 끝까지 밝힌다** — 화면에서도, 계산기로
  넘길 때도, AI 에게 보낼 때도, 인쇄에도.

## 2. 세금은 언제나 조건부

- 「비과세 상품입니다」는 **결론**이다. 결론을 말하면 요건 하나가 어긋났을 때
  고객 앞에서 무너진다. **요건만** 보여 주고 판단은 세무 전문가에게 넘긴다.
- 「**요건 충족 시**」를 빼지 않는다.
- **한도·나이·개월 수 같은 숫자는 적지 않는다.** 시행령에 있고 개정되면
  바뀌는 값이라, 외운 숫자를 말하는 것이 이 자리에서 가장 위험하다.
- 보험료·보장은 「**심사 결과에 따릅니다**」를 빼지 않는다.

## 3. 고객 실명

- **실명은 이 브라우저(localStorage)에만.** 서버에는 `김○○` 마스킹만.
- 폰 캘린더로 나가는 일정은 iCloud·구글로 올라간다 — **이름은 기본 마스킹**.
- 점검·견본 데이터에는 **「홍길동」**을 쓴다. 실제 고객 이름 금지.

## 4. 돈 단위 — 「50만원」이 「50억」으로 찍힌 자리

만원과 원을 섞어 쓰다 **만 배**를 틀렸습니다. 이름이 단위를 말하지 않으면
아무도 모릅니다. 그래서 규약을 정하고 `check-twins.js`가 지킵니다.

| 이름 | 받는 단위 |
|---|---|
| `xxxWon(v)` | **만원** |
| `xxxWonR(v)` | **원** |
| 변수 `*Won` / `won` | **원**을 담는다 |

- 따라서 `xxxWon(yyyWon)`은 거의 언제나 만 배 오류다 → 점검이 잡는다.
- 정말 맞는 경우에만 그 줄에 `「원단위OK」`라고 적어 빠져나간다.
- **필드 이름이 거짓말하게 두지 않는다.** `housePension().won`이 실제로는
  만원을 담고 있어 `.man`으로 고쳤다. 이름이 거짓말하면 부르는 쪽이 틀린다.
- 큰 금액은 **억**으로 적는다. 「50,000만원」은 한 박자 늦게 읽힌다.

## 4-1. 우리가 뽑은 종이를 우리가 다시 읽을 수 있어야 한다

설계사는 이 앱에서 제안서를 뽑고, **그 PDF 를 다시 이 앱에 넣습니다**
(비포&애프터·보장분석). 그래서 인쇄물은 사람만 읽는 것이 아닙니다.

- **`font-feature-settings:"tnum"` 을 켠 채로 인쇄하지 않는다.** 켜고
  브라우저에서 PDF 로 저장하면 **숫자 글자가 유니코드를 잃습니다.** 사람
  눈에는 `3억 5,719만원`인데 프로그램이 읽으면 `억 만원` — 숫자가 통째로
  사라지고 사용자 지정 글자(U+E0xx)만 남습니다. 실제로 그래서 우리가 만든
  제안서를 우리가 못 읽었습니다. 쓰려면 `@media screen` 안에만 두세요.
- 뽑아 낸 글은 **믿을 수 있는지 먼저 묻습니다**(`pdfTextTrust`). 사용자
  지정 글자가 섞였거나 숫자가 거의 없으면 **글이 있어도 그림으로 다시**
  읽습니다. 「글자 4,929자니까 읽었다」 로 넘기면 인쇄 머리글의 날짜
  (`26. 8. 20.`)를 담보 금액으로 집어 옵니다.
- **pdf.js 렌더는 `intent:'print'` 로.** 안 그러면 탭이 뒤로 갈 때 rAF 가
  서서 **에러도 없이 영원히 멈춥니다** — 스피너만 돕니다. 모든 기다림에는
  시간 제한(`pdfWait`)을 겁니다.
- 뽑은 글은 `pdfClean` 으로 씻습니다 — 제어문자·폭 없는 공백·전각 숫자.
- 담보 이름은 **공백을 지운 글**에서 찾고, **금액은 원문에서** 읽습니다.
  둘을 섞으면 옆 칸 숫자가 붙어 `12692295억` 이 됩니다.

`check-pdfread.js` 가 이 전부를 지킵니다 — **실제로 PDF 를 뽑아 다시 읽는
왕복 시험**이 들어 있어, tnum 을 되살리면 그 자리에서 걸립니다.

## 5. 같은 것을 두 곳에 두지 않는다 — 버그가 가장 많이 나온 자리

실제로 이렇게 샜습니다.

- 넘기는 표가 `FP_MAP`·`TOFIN_MAP` **두 벌**이라 한쪽에만 생활비가 있었다
- 트랙을 삼항 사슬로 나열하다 **쿠폰 트랙만 빠뜨렸다** (두 번, 다른 함수에서)
- 트랙이 무엇인지 답하는 곳이 **둘**이라 쿠폰 상담이 **연금으로 기록**됐다
- 칸 모양 CSS 를 **한 화면에서만** 입혀 다른 화면이 벌거벗었다

규칙:

- **표(매핑)는 하나만 두고 다른 곳은 그것을 가리킨다.**
- **탭·트랙을 삼항 사슬로 나열하지 않는다.** 객체 하나로 두면 늘어도
  빠뜨릴 자리가 없다.
- **「지금 어느 화면인가」는 한 곳만 안다.** 다시 세어 답하지 않는다.
- 클래스를 넣어 주는 `xxxCss()`를 쓰는 화면은 **그 화면에서도 부른다.**
- `el.className = '…'`로 통째 대입하지 않는다 — 다른 곳이 붙여 둔 클래스를
  잃는다. `classList.add/remove`를 쓴다.
- **`function X(){}` 를 두고 나중에 `window.X = 다른것` 으로 덮어쓰지 않는다.**
  덮어쓰기 전에 부르는 자리가 있으면 그때는 <b>죽은 판</b>이 돈다. 실제로
  `renderConsultingGuide`·`renderFinalReport` 가 그랬고, 첫 그리기마다
  `metric is not defined` 로 터진 뒤 try/catch 에 조용히 삼켜지고 있었다.
  `check-html` 은 `function` 선언끼리만 견주므로 이것을 못 본다 —
  `check-twins` 가 본다.

### 5-1. 같은 자료를 두 번 받지 않는다 — 철칙

코드만이 아니라 **들어오는 자료**도 마찬가지다. 서재에 같은 약관이 두 벌
쌓이면 홍보 발췌에 같은 조항이 두 번 뜨고, 고객 앞에서 **어느 것이 최신인지
알 수 없게 된다.**

- **이름으로 판정하지 않는다.** 회사가 파일 이름을 바꾸면 그대로 또 들어온다.
  `이름+크기`는 제일 싼 1차 판정일 뿐이다.
- **내용으로 판정한다.** 두 겹이다 —
  ① **바이트 지문**(`sha`) : 똑같은 파일이면 이름이 달라도 안 받는다.
     글자 뽑기 **전에** 걸러 200쪽 약관을 헛읽지 않는다.
  ② **글자 지문**(`tsha`) : 다시 만들어져 바이트는 달라도 내용이 같으면
     안 받는다. 빈칸·줄바꿈·쪽표시는 지우고 잰다.
- **한 묶음 안의 중복도 막는다.** 회사 폴더에 같은 파일이 두 군데 있는 일은 흔하다.
- **지문 없이 예전에 쌓아 둔 것과도 견준다.** 안 그러면 옛 자료와 겹치는 파일이
  한 번은 들어온다. 글자는 이미 손에 있으니 다시 읽지 않는다.
- **무엇과 겹쳐서 안 받았는지 말한다.** 「건너뜀 3건」만 적으면 사장님은
  빠뜨린 줄 안다.
- **헛것을 막지 않는다.** 진짜 다른 문서는 그대로 들어가야 한다. (8번)
- 목록도 같다 — 공시실에서 같은 상품이 두 번 오면 받기 단추도 두 번 뜬다.
- `check-mklib` 이 이것을 지킨다.

## 6. 화면 구조

- 메뉴는 `var TABS` 그룹 → `go(tab)` 라우팅. 두 갈래가 있다.
  - `renderTab(tab)`이 HTML 을 **return**
  - `go()` 안에서 `else if(tab==='x'){dyn.innerHTML=…}` — **`dyn` 은 `$('dynPane')`**
- `app/index.html` 은 **ES5** 로 쓴다(`var`/`function`). 3.1MB · 45,000줄 ·
  함수 2,900개다. `app/finance.html` 과 상담자료 덱은 ES6 를 써도 된다.
- `check-html.js` 는 **같은 이름 함수 중복**을 잡는다. 인자 없이 부르면
  실패하므로 CI 처럼 파일 목록을 넘긴다.
- **PR 을 올릴 때마다 `APP_BUILD` 와 `APP_BUILD_NOTE` 두 줄을 함께 고친다.**
  줄 단위로 통째 교체하십시오 — 문자열 안쪽을 잘라 붙이다 옆줄을 먹은 적이
  있습니다.

## 7. 서버를 아껴 쓴다

무료 한도를 **세 배**로 넘겨 로그인까지 막힌 적이 있습니다. 원인은
「안 보는 화면이 60초마다 본문 120건을 받아 오던 것」이었습니다.

- **되풀이해서 서버를 부르지 않는다.** 꼭 필요하면 `document.hidden` 일 때 쉰다.
- 목록에 미리 다 받아 두지 않는다 — **필요할 때 그 한 줄만** 받는다.
- 화면이 눈에 들어올 때 **한 번만** 부른다(IntersectionObserver).
- `scripts/check-egress.js` 가 이것을 본다.

## 8. 점검을 먼저 쓴다

- 고친 것마다 **점검이 실제로 그 버그를 잡는지 확인한다** — 일부러 되돌려
  보고 빨간불이 켜지는지 본다. 안 울리는 알람은 알람이 아니다.
- **헛것을 잡는 점검은 안 잡는 점검보다 나쁘다.** 사람이 점검을 안 믿게 된다.
  넓게 잡지 말고 확실한 것만 잡고, 예외는 코드에 한 줄 적어 빠져나가게 한다.
  (예: 「상증법 제18조」의 18 을 금액으로 착각하지 않기)
- 새 점검은 **`scripts/checks.tsv` 맨 끝에 한 줄**만 붙인다.
  `갈래<탭>이름<탭>명령` — 갈래는 `fast`(브라우저 없음) 나 `web`.
  **`.github/workflows/check.yml` 은 안 건드린다** — 거기서 매번 부딪혔다.
- **이미 있는 파일 이름을 확인하고 만든다.** `check-tofin.js` 를 덮어써
  기존 점검을 지운 적이 있습니다.

## 9. 자료·인용

- **원수사 약관을 서버나 공개 주소에 올리지 않는다.**
- 남의 자료 삽화를 복제하지 않는다 — **직접 SVG 로 그린다.** 사진은 인쇄에서
  뭉개지고 화면 크기마다 달라지지만, SVG 는 어디서나 같은 선으로 선다.
- 뉴스·통계는 **실제 기사만** 인용한다. 제목·언론사·날짜·링크를 그대로 옮기고,
  **확인 못 한 날짜는 적지 않는다.** 못 받아 오면 못 받았다고 적는다.
- SQL 은 `--` 주석을 쓰지 않는다. `/* */` 만 쓴다.

## 10. 비밀

- `service_role` 키·`SHARED_TOKEN` 을 **채팅·로그·코드에 붙이지 않는다.**
- TLS 검증을 끄거나 `HTTPS_PROXY` 를 지우지 않는다.
- Supabase `anon` 키는 공개용(RLS 보호)이라 진단에 써도 되지만 불필요하게
  반복 노출하지 않는다.

---

## 11. 여럿이 나눠 만들 때 — 갈래

세션을 나눠 동시에 만듭니다. **실제로 됩니다.** 한 세션이 계산기를 고치는
동안 다른 세션이 지도를 새로 그렸고, **둘 다 `app/index.html` 을 건드렸는데도**
줄이 멀어 git 이 알아서 붙였습니다. 45,000줄짜리 파일이라 오히려 유리합니다.

### 갈래

| 갈래 | 맡는 곳 |
|---|---|
| 가. 본체 | `app/index.html` |
| 나. 계산기 | `app/finance.html` |
| 다. 발표 덱 | `app/상담자료/*.html` · `apex-deck.js` |
| 라. 미끼 레이더 | `app/상담자료/미끼레이더/` |
| 마. 서버·소스 | `netlify/functions/` · `config/` |
| 바. 지도 | `app/apex-map*` |
| 사. 재무설계 자료 | `app/재무설계/` (상담자료·실전화법서) |
| 아. 블로그 | `app/blog.html` · `app/blog-art.js` |

점검은 **자기 갈래 것만** 각자 만듭니다 — 파일이 하나씩 따로라 서로 모릅니다.

### 부딪히는 자리는 딱 둘

1. **`APP_BUILD` · `APP_BUILD_NOTE` 두 줄.** PR 마다 고치는 규칙이라, 세션이
   여럿이면 거의 매번 부딪힙니다. 충돌해도 잃을 것이 없으니 **나중 것으로
   덮으십시오.**
2. **`scripts/checks.tsv`.** 새 점검은 **맨 끝에 한 줄**만 붙입니다.
   순서는 상관없습니다 — CI 가 알아서 나눠 돕니다. 한 줄짜리라 부딪혀도
   눈으로 바로 풀립니다. (`check.yml` 은 이제 안 건드립니다.)

그 밖에는 안 부딪힙니다.

### 차례

```bash
git fetch origin main && git checkout -B <내브랜치> origin/main   # ① main 에서 시작
node scripts/lane.js                                             # ② 밀기 전에 한 번
git push -u origin <내브랜치>                                     # ③ 끝나면 바로 민다
git fetch origin main && git merge origin/main                   # ④ 나중 쪽이 받아 붙인다
node scripts/check-twins.js && node scripts/check-sane.js        # ⑤ 붙인 뒤 반드시
```

②의 `lane.js` 는 **점검이 아닙니다** — CI 에 넣지 않습니다. 갈래를 넘는 PR 도
얼마든지 정당하기 때문입니다(계산기를 고치면서 그 점검도 함께 넣습니다).
빨간불을 켜는 대신, 밀기 전에 눈으로 한 번 보라고 적어 줄 뿐입니다.
「헛것을 잡는 점검은 안 잡는 점검보다 나쁘다」(8번)를 지키느라 그렇습니다.

③ **끝나면 바로 미십시오.** 이 컨테이너의 로컬 저장소가 턴 사이에 초기화된
적이 있습니다. 밀어 둔 덕에 원격에서 되찾았습니다.

**먼저 병합한 쪽이 이깁니다.** 나중 쪽이 ④로 받아 붙이고 점검을 다시 돕니다.

### 진짜 위험은 충돌이 아니라 「쌍둥이」다

5번을 다시 봅니다 — 「같은 것을 두 곳에 두지 않는다」. 세션이 여럿이면 이것이
**훨씬 잘 터집니다.** 두 세션이 각자 자기 매핑표를 만들어 넣으면 git 은
**충돌 없이 둘 다 붙여 줍니다.** 빨간불이 안 켜집니다. `FP_MAP`·`TOFIN_MAP` 이
두 벌 되던 자리가 그것입니다. **붙인 뒤에 `check-twins.js` 를 안 돌면 아무도
못 봅니다.**

### 한 파일 안에서도

- **같은 함수는 한 세션만** 만집니다. 이건 git 이 못 잡아 줍니다.
- 새 함수는 **자기가 고치던 자리 가까이** 둡니다. 다들 파일 끝에 붙이면
  거기서 부딪힙니다.

### 세션마다 이렇게 시킨다 (복사용)

> `app/finance.html` 만 만지십시오. 다른 갈래 파일은 열지 마시고, `APP_BUILD`
> 두 줄도 건드리지 마십시오. 새 점검은 `scripts/check-<새이름>.js` 로 만들고
> `scripts/checks.tsv` **맨 끝에** 한 줄을 붙이십시오. 시작 전에
> `git fetch origin main && git checkout -B <브랜치> origin/main`, 밀기 전에
> `node scripts/lane.js`, 끝나면 바로 미십시오.

갈래 이름(`app/finance.html`)만 바꿔서 세션마다 주면 됩니다.

### 값

PR 하나에 CI **5~7분**(전에는 22분이었습니다 — 점검을 여섯 갈래로 나눴습니다).
세 세션이면 PR 셋이 **동시에** 돌아 시계로는 그대로 5~7분입니다.
다만 병합은 한 줄로 서므로, 두 번째부터는 `git merge origin/main` 뒤 CI 를
한 판 더 돕니다 — 그래도 예전 한 판보다 짧습니다.

### 한 파일 안에서 갈래를 더 쪼갠다 — <b>이름 접두사</b>

`app/index.html` 은 57,000줄이라 「가. 본체」 하나로 묶으면 세션이 다 거기
모입니다. git 은 줄이 멀면 알아서 붙여 주지만, **같은 함수를 둘이 만지면**
못 잡아 줍니다. 그래서 파일이 아니라 **이름**으로 나눕니다.

| 세션 | 만지는 이름 | 화면 |
|---|---|---|
| 공지 | `osNtc*` · `osAck*` | 홈 공지 칸 · 설정 |
| 팀 할 일 | `tdo*` | 홈 |
| 메뉴 | `nav*` · `tn*` | 서랍 · 위 띠 |
| 가이드 | `man*` | 사용가이드 |
| 보장분석 | `baba*` · `ba*` | 비포&애프터 · 전·후 만들기 |

**새 함수는 자기 접두사로 만듭니다.** 그러면 이름이 겹칠 일이 없고,
`check-html` 이 이름 중복을 잡아 주므로 실수해도 CI 에서 걸립니다.

---

## 지금 있는 점검 (일부)

| 스크립트 | 보는 것 |
|---|---|
| `check-twins.js` | 선언 없이 쓰는 전역 · 같은 것이 두 곳에 있나 · 돈 단위 규약 · 대소문자 쌍둥이 (정적, 몇 초) |
| `check-sane.js` | 화면에 부서진 값·터진 자릿수·되짚기 어긋남이 있나 |
| `check-html.js` | 문법 · 함수 이름 중복 |
| `smoke.js` | 63개 화면이 실제로 열리나 |
| `check-egress.js` | 서버를 쓸데없이 부르지 않나 |
| `check-oneglance.js` | 연금·투자·상속·부동산·클로징 한 장이 서나 |
| `check-cmsend.js` | 고객 365일 → 계산기로 값이 빈 손으로 가지 않나 |
| `check-ffsame.js` | 두 화면이 같은 질문지를 쓰나 · 평균이 평균이라 밝혀지나 |
| `check-babadrop.js` | 해지가 담보에서 빠지나 · 인쇄가 안 짤리나 |
| `check-trackc.js` | 도해가 직접 그린 것인가 · 뉴스를 지어내지 않나 |
| `check-mklib.js` | 회사 폴더를 통째로 받나 · **같은 자료를 두 번 받지 않나(5-1)** |
| `check-mkpromo.js` | 상품설명서·약관에서 원문 그대로 발췌하나 · 유리한 것만 뽑지 않나 |
| `check-gongsi.js` | 공시실에서 주소만 받아 오나 · **PDF 를 서버가 안 받나(9)** |

전체 목록은 `.github/workflows/check.yml` 에 있습니다.
