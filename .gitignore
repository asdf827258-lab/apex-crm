# Claude Code 로컬 설정(개인 권한·토큰 포함) — 저장소·배포에서 제외
settings.local.json
.claude/

# 점검용으로 내려받는 것들 — CI 가 매번 새로 깐다. 커밋하면 저장소가 통째로 무거워진다.
node_modules/

# OS/에디터 잡파일
.DS_Store
Thumbs.db

# 화면 확인용 임시 사본 — netlify 는 저장소 루트를 통째로 게시하므로,
# 여기 들어온 앱 사본은 라이브 사이트에 한 벌 더 배포된다. 절대 커밋하지 않는다.
_app/
_live/
_serve*/
