# 자동매매 봇 의존성
#   pip install -r requirements.txt

alpaca-py>=0.43        # Alpaca 거래·시세 API (모의계좌 기본)
pandas>=2.0            # 봉 데이터·지표 계산
numpy>=1.24            # pandas 가 쓰지만 지표에서 직접도 쓴다
PyYAML>=6.0            # config.yaml
python-dotenv>=1.0     # .env 에서 키를 읽는다 (코드에 키를 적지 않기 위해)
matplotlib>=3.7        # 자산곡선 그래프 (없어도 백테스트는 끝난다)
pytest>=8.0            # 검증 — 이게 통과하지 않으면 아무것도 돌리지 마세요
